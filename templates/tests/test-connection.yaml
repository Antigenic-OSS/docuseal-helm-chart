{{- if .Values.testConnection.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docuseal.fullname" . }}-test-connection"
  labels:
    {{- include "docuseal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,before-hook-creation
spec:
  containers:
    - name: wget
      image: "{{ .Values.testConnection.image.repository }}:{{ .Values.testConnection.image.tag }}"
      imagePullPolicy: {{ .Values.testConnection.image.pullPolicy }}
      command: ['sh', '-c', 'wget -qO- "http://{{ include "docuseal.serviceName" . }}:{{ .Values.service.port }}/up"']
  restartPolicy: Never
{{- end }}
